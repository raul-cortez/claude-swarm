node_modules/
dist/
out/
*.log
.DS_Store

# icon build intermediates
build/icon.iconset/
build-info.json

# снимки расхода: их пишет строка статуса рядом с собой, в репозитории они появляются
# только когда скрипт гоняют руками из дерева — и несут id чужих разговоров
usage/

# записка вкладки самой себе через перезапуск — своя на каждую вкладку, в репозиторий не едет
.swarm-handoff-*.md
