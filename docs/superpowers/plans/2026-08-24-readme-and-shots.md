# README, скриншоты и «вы» в интерфейсе: состояние на 2026-08-24 (вечер)

Эстафета для свежей сессии. Всё, что описано, уже в `main` и запушено; дерево чистое,
`npm test` зелёный.

## Сделано (4 коммита, все в origin/main)

1. `1e7eab1` — тринадцать кадров интерфейса в `docs/img`, вставлены в README вместо
   плейсхолдеров. Заодно два места, где README расходился с приложением: у панели команд
   нет поиска, а звёздочку «есть правки» из нижней панели убрали ещё в `renderGitBar` —
   теперь там `+N −M`.
2. `145a841` — README пересобран по флоу человека (см. ниже) и получил раздел
   «Когда вас нет: работает без меня» с семью новыми кадрами; два из них встали в
   `MANUAL.md` на размеченные места.
3. `00a36a9` — интерфейс переведён на «вы» (окно отчёта, страница «Авто», гейт владения,
   подсказки, встроенная справка, ответы бота, текстовый отчёт). Правило и просьбы,
   адресованные АГЕНТУ, остались на «ты». Плюс «Прочитано» вместо «Прочитал»,
   «Я за клавиатурой» вместо «Я вернулся», «Правило» вместо «Правила ночи», статусы в
   справке названы цветами.
4. `8fbb484` — MANUAL пересобран по той же нити, что README (поставили → первый агент → пока
   работает → когда ждут несколько → что под рукой → работа без вас → телеграм → долгая
   работа → настройки → клавиши → другие агенты → если что-то не так), везде «вы», «ты» только
   в текстах, адресованных агенту. Добавлены Пульт, гит-панель с диффом, карта настроек по
   восьми страницам, оглавление; «Типичный сценарий» убран как дубль нити. **Сверка настроек с
   кодом нашла три устаревших утверждения в ОБОИХ файлах** (правлены и в README): хуки, своя
   строка статуса и возврат в разговор всегда включены (`hooksEnabled`/`resumeSessions = true`),
   галочки «Просить агента звать вас», поля своих фраз и кнопки «Скопировать правило» нет
   (правило идёт флагом `--append-system-prompt`, `agent-rules.js`), у «где я» три положения и
   иконка стоит всегда. Экшены CI подняты до v5, тесты — на Node 24.

Релиз `v0.42.0` вышел в начале сессии (мак собран локально, `.exe` доложен CI, черновик
снят, манифест отдаёт 0.42.0). Правки из коммитов 2, 3 и 4 в него НЕ попали.

## Что дальше

1. **Спросить человека про релиз 0.43.0.** Установленное приложение говорит «ты», а README и
   MANUAL уже на «вы» и со скриншотами, где «вы». Предложение висит без ответа — переспросить,
   когда поднимешься. Резать минором (обычай репозитория: все версии с 0.37 минорные). Если
   релиз будет — заодно проверить сборку на Node 24 и, если пройдёт, поднять `node-version` в
   `release.yml` (он один остался на 20).
2. **Шесть кадров за человеком:** системное уведомление и пять телеграмных с телефона.
   Список с описанием кадра — `docs/img/README.md`, раздел «Снять живьём». Подделывать их
   отказались осознанно: чужой скрин из сети или нарисованный Telegram видно сразу.
3. **WINDOWS.md всё ещё на «ты»** и не проверялся на устаревшее (50 строк, `install-and-run.bat`).
   DEVELOPMENT.md — про разработку, там «ты» уместно.

## Решения этой сессии (не переписывать, дописывать)

- **Кадры интерфейса снимаются оффскрин-стендом, а не запуском приложения.** Приложение
  поднимать нельзя: разговор живёт в pty самого сворма. Стенд: скрытое `BrowserWindow`
  со своим `userData`, подставной `preload` (contextBridge с выдуманным состоянием вместо
  моста), `loadFile` на настоящий `renderer/index.html`. Дальше `executeJavaScript`
  зовёт функции самого рендерера — `createSession`, `setStatus`, `applyTabAuto`,
  `showSettingsModal`, `openPresenceMenu`, `openDiffOverlay`, `openNightModal`,
  `renderNightPill` — и `capturePage()` пишет PNG в 2x. Файлы стенда лежали в скратчпаде
  сессии и умрут с ней; собираются заново за полчаса. Короткий рецепт продублирован в
  `docs/img/README.md`.
- **Грабли стенда:** текст в терминал печатать только когда вкладка ВИДИМА (скрытая
  мерится в 24 строки, и кадр обрывается на середине); длину сессии подрезать под
  `term.rows`, иначе верх уезжает в скроллбек; сравнивать кадры по копии с новым именем —
  чтение картинки кэшируется по пути; счётчик «N вкладок сами» в стенде надо
  перерисовывать вручную (в приложении это делает пуш из main).
- **Данные на кадрах выдуманные, и это оговорено** в `docs/img/README.md`: нейтральные
  названия проектов, никаких личных путей. Телеграмные кадры выдумывать отказались.
- **Порядок README — по рабочему дню:** установка → первый агент → пока он работает →
  когда ждут несколько → что под рукой при правках → когда вас нет → Telegram →
  настройки → клавиши → другие агенты → что уходит наружу → если что-то не так.
  Раскладка уехала в «Вид», полоска контекста — к статусам. Блоки `<!--DLTOP-->` и
  `<!--DL-->` трогать нельзя: их переписывает `scripts/release.mjs`.
- **Граница «ты/вы»:** человеку — «вы», агенту — «ты». Фраза-метка «Сейчас от тебя» и
  паттерны хука не менялись: это протокол, а не подпись в интерфейсе.
- **Доки проверяются по коду, а не по прошлой доке.** Устаревшие утверждения про настройки
  пережили и переписывание README: пересказ живёт дольше кода. Дёшево ловится грепом по
  `renderer.js` (`set-tab`, `set-check-tx`, `set-label`) — там видно и список страниц настроек,
  и все живые переключатели.

## Чем проверять

- `npm test` — весь набор (единственное, что сломалось от перевода, — утверждение про
  «на тебе:» в `test/night.test.js`, поправлено).
- Вид интерфейса — стендом, измерением, а не глазами. Приложение не запускать.
- Ссылки и картинки в README и MANUAL проверяются скриптом на пять строк: собрать `href="#…"` и
  сверить со слагами заголовков, проверить существование каждого `src="docs/img/…"`.
