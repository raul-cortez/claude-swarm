# Вкладка горит «работает», когда агент давно всё сказал — эстафета

**Состояние:** починка сделана и выпущена в `v0.38.0`, но у человека НЕ УСТАНОВЛЕНА —
крутится `0.36.4` (см. «Висит на человеке»). Хвост про теги закрыт 23 августа отдельным
коммитом. Работа не в середине.
Коммит с правками — `f753221` (плюс часть в `main.js` уехала прошлым коммитом
соседа `d654426`: мы правили дерево одновременно, и он закоммитил всё разом).

Решения, отвергнутые варианты и «почему именно так» — в сообщении `f753221` и в
комментариях у `hookRunStuck` / `trRunStale` в `detector.js`. Ниже только то,
чего в коде не видно.

## С чего началось

Агент в чужой вкладке (`fastio 4`, разговор `01c1ba9d`) закончил ход словами
«Сейчас от тебя: ничего.», а карточка час горела оранжевым. Человек: «не понимаю,
что происходит».

## Что проверено и оказалось НЕ виновато

Это половина работы, и повторять её не надо:

- **Подпись агента.** Прогнал настоящий текст его сообщения через наш разборщик:
  `callKind = null`, зова нет, фона нет. Экран по скриншоту тоже чист — ни
  спиннера, ни рамки, ни счётчика сабагентов.
- **Хук.** В стенограмме сессии записан вывод каждого хука: оба конца хода
  прислали `idle`. Канал отработал верно.
- **`tui: fullscreen`** (он стоит в `~/.claude-my/settings.json` и есть только у
  личных вкладок). Поднял живого Клода в том же конфиге, с теми же
  `--settings`, и через `--resume --fork-session` — маркеры OSC доходят, статус
  зелёный. Полноэкранный режим ни при чём.
- **Разбор маркеров** (`osc.js`) на настоящем потоке — собирается верно.

## Что чинили

Два состояния, из которых нет выхода по времени, — оба лечили:

1. **Стенограмма, замолчавшая на «работает».** Файл сказал «инструмент пошёл» и
   больше не менялся: перечитывать нечего, а экран в этой ветке не спрашивают.
   Так выглядит вкладка, привязанная к чужому или брошенному разговору.
2. **Сигнал `busy` от хука.** Срока у него не было вовсе: оборвался канал посреди
   хода — оранжевый навсегда.

Плюс: адрес разговора, названный хуком, теперь исправляет уже сделанную привязку
(раньше его спрашивали только у вкладки без файла), и появился **`status.log`** —
каждая смена цвета с показаниями всех трёх каналов одной строкой.

## Незакрытый хвост

**Какая именно из двух дыр держала ту вкладку — не доказано.** В работающей
сборке нечем спросить приложение: журнала статуса тогда не было, а активную пробу
(дописать строку в мёртвую стенограмму и посмотреть, кто отзовётся) заблокировал
классификатор — и правильно, это чужие данные. Косвенная улика в
`transcript.log`: сегодня **две вкладки держали одну стенограмму**
(`17413726…`, корпоративный конфиг), в которую последний раз писали в 11:44, а
разговор `01c1ba9d` не держал никто. Обе дыры закрыты; если повторится —
`status.log` рядом с настройками назовёт виновника сразу.

## Висит на человеке

**Поставить свежую версию** (на 23 августа это уже `v0.39.0`, см. ниже). Спрошено 23
августа про `v0.38.0` — ответ «позже, сначала доделаем».
В `payload/` рядом с настройками лежит один файл, `0.36.4.asar` от 18 августа, и
приложение поднято 21-го: значит НИ ОДНА починка из `f753221` и `v0.38.0` сейчас не
работает, `status.log` в папке настроек даже не создан, а теги мертвы — живой
`swarm-phrases.json` содержит только фразу, полей под теги в нём нет. Пока это так,
залипший оранжевый по журналу не разобрать: журнала нет.

Установка = `app.relaunch()`, то есть гибель всех вкладок разом, и это единственная
причина, по которой он тянет. Устройство обновления её не требует: новый файл кладётся
отдельно от установленного приложения и подхватывается загрузчиком (`bootstrap.js`), а
`update:apply` и `update:relaunch` — уже два разных IPC. Кнопка просто вызывает их подряд
(`renderer.js`, ветка `!useInstaller`). **Отложенная установка** — «скачать сейчас,
применить при следующем запуске» — стоит одной кнопки и снимает вопрос навсегда.
Предложено, не выбрано.

## Выпущено 23 августа в `v0.39.0`

Собрано и опубликовано: `.dmg`, `.exe`, `app.asar`, `manifest.json`, CI снял черновик,
обновлялка версию видит. С этого момента метка и обе починки залипшего оранжевого
существуют для всех — **кроме самого человека: у него по-прежнему `0.36.4`.**

Про отложенную установку он сказал «сначала релиз». Релиз есть, срок наступил —
спросить заново. Подробнее, зачем она и что для неё уже готово, — в разделе «Висит на
человеке» выше и в `2026-08-23-tab-density-and-layout.md`.

## Теги: закрыто 23 августа

Решение принято с человеком: метка — `[swarm:вопрос]` / `[swarm:фон]`, считается
**с начала строки**, учим ставить первой строкой сообщения. Приставка против случайного
слова, начало строки — против намеренного упоминания (агент, пишущий про сам протокол,
себя больше не зовёт). Короткая форма `[вопрос]` ещё понимается: её могли выучить из
чужих CLAUDE.md.

Чего в коде не видно, а знать стоит:

- **Позиция ради экрана не нужна.** Человек предположил, что тег обязан быть в конце,
  «чтобы попасть в экран». Нет: экран видит переписку целиком, границ сообщений в нём
  нет, и работает не позиция, а «последняя метка побеждает». Правило «с начала строки»
  накрывает и первую строку, и последнюю — выбирать не пришлось.
- **А вот маркер `⏺` — нужен, и на нём правило чуть не сломалось молча.** Claude Code
  печатает первую строку своего сообщения как «⏺ текст». Тег первой строкой достаётся
  экрану как `⏺ [swarm:вопрос]`, и без поправки в `TAG_BULLET` он находился бы только в
  середине и в конце — то есть вкладка без хуков не звала бы, и увидеть это можно было бы
  только вживую. Разбор построчно тут не выход: зов ищется по всему снимку сразу
  (`screen.js asksForInput`), мебель из него не вычищена.
- **Выжимку пришлось развернуть.** Она резала от найденной метки — при теге сверху в
  уведомление уезжало начало отчёта. Теперь при теге берётся конец сообщения: тег
  говорит «я жду», а вопрос агент дописывает последним абзацем.
- **Поле проверки в настройках стало многострочным.** Однострочный `input` съедал
  переводы строк и отвечал бы «не позовёт» там, где живой агент зовёт.
- **Отпечаток зова (`screen.js askFingerprint`) проверен, менять не стал.** С тегом все
  зовы выглядят одинаково, и казалось, что второй вопрос подряд будет читаться как уже
  отвеченный. Не будет: пока оба тега на экране — отпечаток меняется числом строк, а
  когда старый уезжает, `asksNow` обнуляет память об ответе (`detector.js`).
- **Остаточная шероховатость, осознанная.** Агент, который пишет доку про протокол и
  ставит пример тега с начала строки в самом сообщении, себя позовёт. Раньше хватало
  любого упоминания; теперь нужно именно начало строки. Ложный зов виден и снимается
  взглядом — молчание было бы дороже.

## Грабли и как проверять

- **Приложение не запускать** (`npm start`, `electron .`): разговор живёт в pty
  самого сворма. Проверка — `npm test` (зелёный целиком) и чтение журналов в
  папке настроек: `status.log`, `transcript.log`, `telegram.log`.
- **В дереве работают соседние агенты.** `git add -A` не делать, коммитить только
  своими путями — иначе унесёшь чужое незаконченное (сегодня так и вышло, только
  в обратную сторону).
- **Живой стенд вместо догадок.** Точная копия детектора поверх настоящего Клода
  собирается за десять строк: `node-pty` + `@xterm/headless` из `node_modules`,
  кормить `extractHookSignals` и `tickStatus`, печатать вердикт раз в секунду.
  Это единственное, что за весь разбор отвечало на вопросы прямо, а не косвенно.
