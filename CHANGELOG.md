# Changelog

## 0.48.0 — 2026-08-24

- feat(auto): плашка над отданной вкладкой вместо модалки
- fix(auto): мышь и ответы терминала перестали быть печатью

## 0.47.0 — 2026-08-24

- fix(night): за метку отвечает сворм, а не человек
- fix(settings): подсказка про метку объясняет метку, а не второе её написание
- fix(settings): метка вопроса больше не выдаётся за настройку
- fix(settings): заготовку ночного режима стало можно править, проза во всю ширину
- fix(ui): переключатель «где я» говорит по-людски, третье положение — «ночной режим»

## 0.46.0 — 2026-08-24

- fix(ui): подсказка пустой сцены заговорила по-русски
- fix(ui): две записки на пустой сцене и молчаливое закрытие окна

## 0.45.0 — 2026-08-24

- fix(status): стрелка в прозе больше не читается как рамка выбора

## 0.44.0 — 2026-08-24

- fix(ui): кнопки на странице «Обновления» встали вровень
- revert(update): момент обновления снова выбирает человек

## 0.43.0 — 2026-08-24

- fix(ui): полумесяц под курсором горит в полную силу
- feat(ui): «ночной режим» вместо «авто», и тексты больше не считают всех маководами
- feat(ui): полумесяц на карточке вместо невидимого правого клика, ночная карточка целиком фиолетовая
- fix(ui): интерфейс говорит с человеком на «вы»

## 0.42.0 — 2026-08-24

- fix(status): вкладка с вопросом и подагентом внутри перестала мигать подписью
- feat(restart): агент зовёт перезапуск сам — и знает об этом с первой строки
- fix(auto): отчёт — про вкладки, которые отдавали, а не про весь сворм
- fix(review): десять находок ревью, включая мёртвый рендерер
- feat(auto): мандат «работай без меня» вкладке, а не времени суток
- fix(settings): предпросмотр карточек показывает черновик, а не то, что применено
- feat(settings): ночной режим — свой раздел и свои формулировки; своя строка в телегу стала выбором
- fix(night): «вкладка закрылась» больше не пишется на выходе из приложения
- feat(night): утренняя сводка — карточка на вкладку, а не разделы по роду события
- fix(night): поводом для ночного вопроса стал кончившийся ход, а не простой

## 0.41.0 — 2026-08-23

- feat(update): обновление кладётся в фоне, перезапуск — отдельно и по желанию

## 0.40.0 — 2026-08-23

- refactor(settings): блок «Как агент зовёт вас» снят; проза больше не моноширинная
- fix(tabs): компактная плотность правда компактная, а раскладка ждёт «Сохранить»

## 0.39.0 — 2026-08-23

- fix(status): метка зова — [swarm:вопрос] с начала строки, а не любое вхождение

## 0.38.0 — 2026-08-23

- fix(tabs): у открытой вкладки нет своего цвета — она цвета своего статуса
- fix(status): «работает» больше не живёт вечно — ни у хука, ни у стенограммы
- fix(settings): подпись у кнопки правила называет настоящие случаи
- feat(status): агент помечает конец хода тегом, а не фразой
- fix(settings): проверка фразы читается как проверка, объяснение — под вопросик
- feat(settings): шесть настроек под нож, статус карточки — один выбор
- feat(settings): тема — селектом и одним предпросмотром, а не плитками
- feat(settings): разделы слева, системный шрифт и группы вместо плоского списка

## 0.37.0 — 2026-08-22

- fix(night): стена лимита — по настоящим строкам Клода, а не по памяти
- fix(night): круг ревью — девять правок, две из них меняли поведение целиком
- feat(night): ночной режим, утренняя сводка и лимиты, которые агент наконец видит

## 0.36.5 — 2026-08-21

- fix(ctx): полоска контекста больше не верит проценту из прозы агента

## 0.36.4 — 2026-08-15

- feat(restart): перезапуск зовут файлом в любой момент — и из немоты, и до порога

## 0.36.3 — 2026-08-14

- fix(restart): разрешение, положенное после истёкшей отсрочки, больше не лежит мёртвым
- fix(restart): вкладка больше не запирает себя собственной служебной перепиской

## 0.36.2 — 2026-08-13

- feat(restart): вкладку с непрочитанным ответом не гасим, пока человек не увидит
- fix(status): шаг фонового подагента больше не гасит открытый вопрос

## 0.36.1 — 2026-08-12

- fix(status): фон не гаснет ни от напоминания, ни в споре каналов

## 0.36.0 — 2026-08-11

- feat(restart): агент зовёт перезапуск сам, не дожидаясь конца отсрочки
- fix(pty): длинный текст больше не вешает приложение целиком

## 0.35.0 — 2026-08-11

- feat(restart): просьба напоминает сохранить наработки по процессу
- fix(restart): рамка уходит и молча, а напоминание — не событие
- feat(restart): в записку уходят и хвосты разговора, а не только работа
- fix(restart): рамку теряли там, где она как раз и стоит
- fix(restart): рамку отличает сам Клод, а не догадка по «ждёт»
- fix(restart): проход ревью — коробка вопроса, пинок и объявленный вопрос
- fix(restart): ожидание фоновых агентов не ведёт вкладку к немоте
- fix(statusline): проход ревью — перенос строки, ранний процент и висячая чужая команда
- fix(restart): с фоновыми агентами не рвём, а договариваемся
- feat(statusline): наша строка дописывается к вашей, а не заменяет её
- feat(restart): просьба велит перенести незаданный вопрос в эстафету
- fix(restart): перезапуск идёт по событию и не ждёт, пока вкладка позовёт

## 0.34.0 — 2026-08-11

- fix(launch): под «+» два пункта без подсказок, режим выбирает окно
- feat(restart): вкладка говорит, если запуск после перезапуска не поднялся

## 0.33.1 — 2026-08-11

- fix(restart): третий проход ревью — pgrep не видит наши процессы, спрашиваем ps
- fix(restart): второй проход ревью — сбой опроса больше не значит «пусто»
- fix(restart): проход ревью — свобода оболочки вместо смерти одного номера
- fix(restart): винда закрывает агента так же, как мак, а не печатает /exit
- fix(restart): агента закрываем сигналом, а не печатаем ему /exit

## 0.33.0 — 2026-08-11

- feat(settings): единая сетка панелей, подсказки — в тултип по вопросику
- fix(launch): под кнопкой всегда два пункта, список подписок — в окне выбора
- fix(launch): пункты меню названы действием, а не именем команды

## 0.32.0 — 2026-08-10

- fix(launch): второй проход ревью — вкладку без строки запуска не спрашиваем
- fix(launch): проход ревью — заряженный перезапуск и фокус после отмены
- feat(launch): «+» на папке разворачивает выбор команды
- feat(status): вкладка с живой фоновой задачей больше не зелёная
- fix(restart): седьмой проход ревью — одна проверка живого агента на оба решения
- fix(restart): шестой проход ревью — разрешение портится целиком, а не наполовину
- refactor(restart): решения — в автомат под тестом, main.js остаётся руками
- fix(restart): четвёртый проход ревью — вернул слепую выдержку и потолок молчания
- fix(restart): третий проход ревью — тишина перед /exit и агент, которого нет
- fix(restart): второй проход ревью — от общего файла до слепого Enter
- fix(restart): правки по ревью — от порога по умолчанию до брошенного /exit
- feat(restart): вкладка сама переживает конец контекста
- feat(settings): своя строка статуса Клода или наша — выбор, а не данность
- feat(readme): в шапке настоящие кнопки скачивания вместо декоративных плашек
- fix(readme): бейдж релиза не отрисовывался — кириллица в строке запроса

## 0.31.0 — 2026-08-07

- feat(install): npx claude-swarm — одна команда вместо списка способов
- feat(install): установка одной командой и сборка к себе, обе без блокировки
- feat(update): обновление кладётся рядом с настройками, а не внутрь приложения
- fix(tabs): вкладка больше не наследует чужую Клод-сессию и не теряет журнал
- feat(boot): код обновления переезжает наружу, бандл больше не трогаем

## 0.30.0 — 2026-08-07

- fix(telegram): пароль от мака больше не спрашивают на каждом запуске
- feat(logs): красный «!» гаснет, когда лог прочитан, и убирается кнопкой
- fix(update): пропавшая связь больше не выглядит поломкой приложения
- feat(telegram): команды Клода набираются с телефона, а не уходят агенту прозой
- fix(tabs): вкладка, запущенная алиасом руками, остаётся своим аккаунтом

## 0.29.0 — 2026-08-03

- feat(telegram): разметка ответа доезжает до чата, а не звёздочками

## 0.28.0 — 2026-08-03

- fix(telegram): «полчаса, дальше отпущу» читалось наоборот
- feat(telegram): в режиме компа мост в вкладки не пишет — разворачивает кнопкой

## 0.27.3 — 2026-08-03

- fix(status): «Сейчас от тебя: ничего» жирным — больше не зов

## 0.27.2 — 2026-08-02

- fix(tabs): алиас агента больше не подменяется на claude

## 0.27.1 — 2026-08-01

- fix(tabs): папку больше не утащить за кнопку «＋»

## 0.27.0 — 2026-08-01

- feat(telegram): скриншот в тему уходит агенту, а «где я» больше не прячется

## 0.26.3 — 2026-08-01

- fix(hooks): ушёл с телефоном — агент спрашивает прозой, а не коробкой с вариантами
- fix(telegram): счётчик токенов виден и в ходах, начатых кнопкой из чата

## 0.26.2 — 2026-07-31

- fix(status): разговор вкладки ищем там, где он есть, а не в одном ~/.claude
- fix(telegram): живая строка в «думаю…» — теперь видно, идёт она или нет
- fix(telegram): вопрос с вариантами доходит кнопками, а из тупика есть выход
- fix(tabs): чем вкладка занята — смотрим, а не угадываем
- fix(status): оборванный ход не держит вкладку жёлтой после перезапуска

## 0.26.1 — 2026-07-31

- fix(status): один разговор — одной вкладке, и он переживает перезапуск
- fix(tabs): вкладка запоминает агента, которым её действительно запустили

## 0.26.0 — 2026-07-31

- feat(ui): «за телефоном» — вся нижняя панель янтарная
- fix(ui): пилюли нижней панели наконец своего цвета

## 0.25.7 — 2026-07-31

- feat(telegram): «думаю…» с живыми числами — время, инструмент, токены, контекст
- fix(telegram): огрызки вместо вопросов — молчим, пока у вкладки нет настоящего текста
- fix(telegram): кнопки разрешения — по одной в ряд, всегда
- fix(telegram): длинный вариант разрешения читается целиком
- fix(status): «работает» больше не ждёт 2.5 с, когда о нём сказал хук

## 0.25.6 — 2026-07-31

- fix(telegram): «за компом» — это тишина, а краткость остаётся настройкой
- feat(telegram): два положения «где я» + управление ими и последним ответом из бота
- feat(ui): вместо «отошёл»/«меня нет» — где я сейчас: за компом, с обоих, за телефоном

## 0.25.5 — 2026-07-31

- fix(ui): ручка папки не раздвигает заголовок на ховере
- Revert "feat(ui): ручка переноса — полоса по верхней кромке карточки и папки"

## 0.25.4 — 2026-07-31

- fix: точный статус через хуки был выключен у всех — пробел в пути к скрипту

## 0.25.3 — 2026-07-31

- fix: прокрутка внутри Клода не двигает статус — детектор не верит отлистанному экрану

## 0.25.2 — 2026-07-31

- fix: прокрутка колесом не уходит в pty и больше не двигает статус

## 0.25.1 — 2026-07-31

- fix(ui): вход в пульт снимает подсветку с вкладки сессии

## 0.25.0 — 2026-07-30

- feat(ui): ручка переноса — полоса по верхней кромке карточки и папки

## 0.24.1 — 2026-07-30

- fix(ui): иконка «отошёл» — настоящая дверь, а не дверь без рамы

## 0.24.0 — 2026-07-30

- feat(telegram): кнопка «отошёл» вместо догадки по простою мыши

## 0.23.1 — 2026-07-30

- fix: вкладка стартует чистой, без нашей служебной строки на пол-экрана

## 0.23.0 — 2026-07-30

- feat: приложение называется Swarm
- refactor: имя репозитория одним местом — package.json

## 0.22.0 — 2026-07-30

- fix(release): changelog считать только от версионных тегов
- feat(release): выпуск и CI на гитхабе, лицензия MIT
- feat(update): ходить по редиректам и качать без токена

## 0.21.4 — 2026-07-30

- fix(status): оборванный на полуслове агент больше не висит «работает»

## 0.21.3 — 2026-07-30

- fix(status): отвеченный вопрос больше не зовёт снова и не приходит уведомлением с твоим же ответом

## 0.21.2 — 2026-07-30

- fix(telegram): ответ уходит целиком, а перенос по ширине окна — не перевод строки

## 0.21.1 — 2026-07-30

- fix(telegram): в чат уходит весь ответ хода, и один итог не приходит дважды

## 0.21.0 — 2026-07-29

- feat(telegram): синк тем в обе стороны, выбор подробности ответов и мастер настройки

## 0.20.2 — 2026-07-29

- fix(telegram): в журнале моста видно, кто написал и кто нажал

## 0.20.1 — 2026-07-29

- fix(telegram): клик мышью отрезал ответ, а спросивший с телефона его не получал

## 0.20.0 — 2026-07-29

- feat(telegram): /usage — расход контекста и лимитов подписки в чат

## 0.19.0 — 2026-07-29

- feat(запуск): вторая копия не запускается, а открывает вкладку в своей папке

## 0.18.0 — 2026-07-29

- feat(запуск): вкладка может стартовать в нужном режиме разрешений
- chore(release): красная сюита останавливает выпуск

## 0.17.0 — 2026-07-29

- fix(ui): ручка переноса в шапке папки всегда занимала место в заголовке
- fix(screen): в уведомлении вместо вопроса приходили полоски
- feat(status): агента просим звать вас сами, править CLAUDE.md не нужно
- fix(telegram): кнопка попадала в чужого агента, а итог хода — в прошлую задачу

## 0.16.3 — 2026-07-29

- fix(resume): новая вкладка после перезапуска упиралась в «сессия не найдена»

## 0.16.2 — 2026-07-29

- fix(macos): обновление ломало подпись приложения, и оно каждый старт просило пароль

## 0.16.1 — 2026-07-29

- fix(telegram): один и тот же запрос разрешения приходил дважды
- fix(telegram): /mode auto включал «правки без спроса» вместо авто

## 0.16.0 — 2026-07-29

- feat(telegram): расшифровка голосового приходит отдельным сообщением до ответа
- fix(telegram): /mode auto молчал «не разобрал режим» вместо объяснения
- fix(telegram): обрыв сети терял запрос разрешения и падал ошибкой в лог
- fix(screen): в запросе разрешения не было видно, что именно просит агент
- feat(telegram): быстрые команды — меню у поля ввода и кнопки в шапке темы

## 0.15.0 — 2026-07-29

- feat(ui): панель Телеграма переписана глазами человека, который видит её впервые
- fix(macos): пароль от связки ключей просили дважды и даже без настроенного моста
- fix(telegram): в чат уезжал огрызок ответа вместо всего хода
- feat(telegram): одно сообщение на ход вместо стрелочки и /mode для режима разрешений

## 0.14.5 — 2026-07-29

- feat(telegram): закрытая вкладка теперь сносит свою тему, а не запирает её
- fix(transcript): вкладка из телеги отбирает свой файл у того, кто держит его зря

## 0.14.4 — 2026-07-29

- fix(telegram): в чат уезжала рамка и собственный вопрос пользователя

## 0.14.3 — 2026-07-29

- fix(transcript): вкладка навсегда оставалась без стенограммы, и итог уезжал пустым

## 0.14.2 — 2026-07-29

- feat(telegram): журнал показывает, почему итог хода не уехал в чат

## 0.14.1 — 2026-07-28

- feat(telegram): журнал моста пишется всегда и открывается кнопкой
- fix(screen): запрос разрешения не разбирался — бот отвечал «вариантов не разобрал»
- fix(telegram): сообщение из телеги ложилось в поле ввода и там оставалось

## Не выпущено

- feat(statusline): расход 5-часового и недельного лимита подписки в строке статуса

## 0.14.0 — 2026-07-28

- test(ui): опечатка в id элемента больше не убивает панель настроек молча
- build(voice): скрипт, которым сборки whisper.cpp попадают в реестр
- feat(voice): голосовые включаются одной кнопкой, без консоли и путей
- fix(ui): настройки обещали, что разрешение подтверждают только за компьютером

## 0.13.0 — 2026-07-28

- fix(telegram): голосовое больше не может остаться без ответа
- fix(telegram): закрытие вкладки не убирало за собой в телеге
- fix(telegram): новая группа получала ответы, адресованные прошлой
- fix(transcript): на Windows стенограмма искалась в папке, которой нет
- fix(screen): кнопка разрешения могла одобрить не то, что на ней написано
- feat(telegram): голосовые распознаются локально, эхом и только потом агенту
- feat(telegram): /new в теме и зеркало итогов, когда тебя нет за маком
- feat(telegram): разрешения одобряются кнопками из телеги
- feat(telegram): кнопки под запросом разрешения и приём нажатий
- feat(screen): разбор запроса разрешения в данные + отпечаток
- docs(plan): второй заход моста — разрешения кнопками, голос локально, /new, зеркало итогов
- fix(telegram): один путь привязки, мёртвый код не висит на экране, модалка шире
- feat(telegram): /sync — привести темы в соответствие с открытыми вкладками
- fix(ui): ручка перетаскивания не отбирает место у имени вкладки
- fix(screen): служебные строки Claude Code больше не уходят как вопрос
- feat(transcript): две вкладки в одной папке разводятся по экрану
- feat(resume): вкладка возвращается в свой диалог по точному id сессии
- fix(telegram): темы создаются сразу для всех вкладок, а не когда агент заговорит
- fix(telegram): привязка молчала, если код истёк, пока выдавали боту права

## 0.12.0 — 2026-07-28

- docs: телеграм-мост и новые модули в README
- feat(telegram): мост требует группу с темами — вкладка живёт в своей теме
- feat(telegram): режим телеги — агент знает, что отвечает на телефон
- fix(telegram): не терять отказ отправки в промисе привязки
- feat(telegram): вопросы агентов в телефон, ответ реплаем в живую сессию
- feat(telegram): токен в настройках, привязка чата QR-кодом
- feat(telegram): протокольный слой моста — long polling, привязка кодом, разбор апдейтов
- feat(status): стенограмма Клода — рабочий источник статуса и текста вопроса
- feat(status): проба стенограммы Claude — третий канал, пока только в лог
- chore(build): не просить у macOS камеру, микрофон и Bluetooth
- fix(ui): подсветка строк диффа не обрывалась при горизонтальном скролле
- feat(ui): карточки и папки перетаскиваются за ручку слева
- feat(ui): пульт объявляет, кому передал терминал
- fix(status): вкладка выходит из «ждёт» сразу после ответа, а не через 5 секунд
- feat(status): настраиваемые фразы «агент зовёт» + Stop по тексту реплики

## 0.11.0 — 2026-07-24

- docs(plan): план гибридного статуса (хуки + экран) и замка «ждёт»
- fix(status): снимок экрана съезжал в пустой хвост буфера — ложный «готов»
- fix(status): «Сейчас от тебя: ничего, жди …» не триггерит «ждёт»
- docs: гибридный статус (хуки + экран) и различение разрешение/вопрос
- feat(hooks): opt-in установка хуков через --settings (без глобального конфига)
- feat(hooks): скрипт swarm-signal + формат маркера OSC 777 notify
- feat(status): OSC-сигналы хуков + арбитраж с экраном
- feat(status): разрешение/вопрос в пульте, подписи вкладки и уведомлении
- refactor(status): вынести детектор в тестируемый detector.js
- feat(status): замок «ждёт» для режима без хуков
- feat(status): inferWaitingKind — разрешение/вопрос из снимка

## 0.10.0 — 2026-07-23

- fix(ui): стабильная геометрия вкладок — не прыгают по высоте/ширине

## 0.9.0 — 2026-07-23

- feat(ui): скролл настроек, раскладка в настройках, чистка пульта, плюс в потоке
- fix(status): ждущая вкладка зеленела при сворачивании папки

## 0.8.1 — 2026-07-23

- fix(build): screen.js не попадал в asar — сборка не стартовала

## 0.8.0 — 2026-07-23

- feat(statusline): прогрессбар контекста из коробки для всех юзеров
- feat(status): оранжевый статус и значок при работе сабагентов
- fix: правки по ревью слитых фич (дифф/пульт/тест-контракт)
- feat(terminal): кликабельные ссылки по ⌘/Ctrl+клик с подсказкой
- feat(diff): дифф вкладки — счётчик +N −M и оверлей в стиле GitLab MR
- feat(tabs): панель настроек визуала вкладок с предпросмотром
- feat(pult): пульт ждущих агентов над терминалом
- feat(tabs): пресеты плотности и классы видимости элементов
- refactor(tabs): габариты карточки в CSS-переменных
- feat(tabs): загрузка и применение tabstyle (вид пока не меняется)
- feat(tabs): модуль tabstyle — данные и валидация визуала вкладок
- fix(tabs): кнопка новой сессии в top-раскладке ужата до иконки
- fix(tabs): свёрнутая группа не растягивается по высоте соседей
- feat(status): детект «ждёт ответа» по строке «Сейчас от тебя …»

## Unreleased

- feat(terminal): ссылки http(s) в терминале открываются в браузере по ⌘+клик (Ctrl+клик на Windows/Linux); при наведении — подсказка о комбинации
- feat(diff): счётчик изменений `+N −M` в статус-баре рядом с веткой; считает незакоммиченное, включая новые файлы
- feat(diff): клик открывает просмотр изменений — дерево тронутых файлов и красно-зелёный дифф. Только чтение, правка — кнопкой «открыть в редакторе»
- feat(diff): маркер `*` (есть незакоммиченное) убран — счётчик говорит то же точнее

## 0.7.0 — 2026-07-22

- feat(launch): режим «чистый терминал» + опция в выборе агента
- feat(launch): несколько сохранённых команд запуска + выбор агента при открытии вкладки
- docs: план реализации пульта + правки спека
- docs: пульт отключается в настройках, хоткей ⌘0 вместо ⌘1
- docs: спек пульта — очередь ждущих агентов над живым терминалом
- docs: план реализации настроек визуала вкладок
- docs: спек настроек визуала вкладок
- docs: план реализации диффа вкладки
- docs: счётчик диффа переезжает в статус-бар к ветке
- docs: спек диффа вкладки (счётчик + оверлей в стиле GitLab MR)

## 0.6.22 — 2026-07-15

- feat(resume): opt-in восстановление диалогов Claude после перезапуска

## 0.6.21 — 2026-07-11

- feat(keybinds): модификаторы слово/край строки вместо отдельных аккордов

## 0.6.20 — 2026-07-11

- fix(mac): spawn-helper path после asar-swap без поломки Win

## 0.6.19 — 2026-07-11

- fix(win): грузить node-pty из app.asar.unpacked после asar-swap

## 0.6.18 — 2026-07-11

- fix(update): убрать native из публикуемого asar — Win снова видит conpty

## 0.6.17 — 2026-07-11

- fix(ui): прогресс и блокировка кнопки при скачивании установщика

## 0.6.16 — 2026-07-11

- chore: маркер для чистого Win asar-апдейта после 0.6.15

## 0.6.15 — 2026-07-11

- chore: маркер in-place ok в настройках — для проверки asar-апдейта на Win

## 0.6.14 — 2026-07-11

- fix(win): не валить asar-апдейт из-за exit-кода cmd после start

## 0.6.13 — 2026-07-11

- fix(win): helper launch — не del себя в .cmd (ERRORLEVEL 1)

## 0.6.12 — 2026-07-11

- fix(status): детект «ждёт ответа» по стрелке выбора, не только ❯

## 0.6.11 — 2026-07-11

- fix(ui): «ждёт ответа» — жёлтая стрелка вместо teal-точки

## 0.6.10 — 2026-07-11

- ci(windows): качать node-pty через curl с ретраями, без allow_failure

## 0.6.9 — 2026-07-11

- fix(win): запускать asar-swap helper через cmd start — вне Job Object

## 0.6.8 — 2026-07-11

- fix(mac): отложенный asar-swap после выхода — без диалога о краше

## 0.6.7 — 2026-07-11

- fix(win): надёжный deferred asar-swap — retries + EncodedCommand

## 0.6.6 — 2026-07-11

- fix(mac): About показывает версию из asar после апдейта

## 0.6.5 — 2026-07-11

- fix(win): короткий description — иначе NSIS ломает иконку ярлыка на рабочем столе

## 0.6.4 — 2026-07-11

- fix(win): AppUserModelId + иконка окна из asar; свежий manifest при апдейте; без автоскролла

## 0.6.3 — 2026-07-11

- feat(настройки): кнопка обновления + хоткеи под Windows

## 0.6.2 — 2026-07-11

- fix(win): asar-swap после выхода процесса — без EBUSY

## 0.6.1 — 2026-07-11

- fix(win): BMP-иконка для Wine/rcedit + иконка окна

## 0.6.0 — 2026-07-11

- fix(win): иконка .ico + build-info в CI для апдейтера

## 0.5.0 — 2026-07-11

- feat(клавиши): настраиваемые хоткеи ввода + sticky scroll

## 0.4.0 — 2026-07-09

- feat(апдейт): релиз публикует app.asar + manifest.json, build-info с runtimeId
- feat(апдейт): плашка «Обновить» + модалка + вкладка «Обновления» в настройках
- feat(апдейт): IPC в main + методы моста в preload + relocation при старте
- feat(апдейт): эффекты — проверка, скачивание+swap asar, self-relocation
- feat(апдейт): чистая логика решения об обновлении + тесты
- docs: план реализации авто-обновления
- docs: спека авто-обновления (asar-swap + self-relocation + Windows)

## 0.3.1 — 2026-07-09

- ci(windows): --publish never + авто-ретрай — чинит 404 на .exe в релизе

## 0.3.0 — 2026-07-09

- feat(логи): трекинг ошибок — красный «!» в баре + модалка логов с копированием
- fix(preload): вернуть clipboardWrite + onMenuCopy — краш всего UI на старте

## 0.2.0 — 2026-07-09

- fix(раскладка): в режиме «табы сверху» светофоры в верхней полосе, табы прижаты влево
- feat(окно): подтверждение при закрытии приложения (агенты завершатся)
- feat(тема): применение вида + вкладка «Вид» в настройках (пресеты, шрифт, курсор, предпросмотр)
- feat(тема): пресеты палитр + чистая нормализация настроек вида
- docs: план реализации темы терминала + конфирм закрытия
- docs: спека темы терминала (пресеты + шрифт/курсор)

## 0.1.1 — 2026-07-08

- fix(release): использовать кэшированный Electron при сборке .dmg
- ci: GitLab-пайплайн + release-скрипт (Mac локально, Windows в CI через Wine)
- refactor(сайдбар): действия в нижний бар (VS Code style), «+» под вкладками
- refactor(уведомления): убрать дубль 🔔 из сайдбара + опция «активная вкладка»
- fix(настройки): кнопка ⚙ как у соседей + вкладка «Уведомления» в модалке
- feat(запуск): панель настроек (команда агента + флаги) и фикс уведомлений
- feat(уведомления): тихо при активном окне и первые 10с после старта
- fix(mac): «swarm»/карточки больше не налезают на светофор
- fix(git-бар): кроссплатформенность под Windows
- feat(git-бар): ветки по свежести, справка и понятный диалог входа в Git
- feat: git-бар снизу — текущая ветка, переключение, fetch/pull (в стиле VS Code)
- chore: сборка под Windows (nsis, afterPack), доки и вспомогательные скрипты
- docs: план реализации git-бара снизу
- docs: спека git-бара снизу (выбор/переключение ветки в UI)
- feat: запоминание команды — не только claude*, но и агент-лончеры (glm/deepseek/…)
- fix: фоновая вкладка сразу показывает «ждёт ответа» на промпте
- feat: запоминание claude-команды + фиксы терминала, скроллбара и навигации
- fix: убираем ложный «Готов» — тихая пауза при работе больше не читается как простой
- fix: тёмный скроллбар терминала вместо белой полосы справа + пады сцены
- fix: давим ложное «работает» — буфер оранжевого, грейс ввода, мин-длительность пуша
- feat: дашборд-апгрейд — иконки, контекст на карточках, папки-группы, DnD, команды
- feat: упаковка + брендинг + панель команд + перекройка вкладок (папки, statusbar)
- feat: системные уведомления от фоновых агентов + анти-ложняк на перерисовках
- feat: запуск сессии в выбранной папке (folder picker)
- feat: переименование карточек (двойной клик, многострочность) + guard disposed-frame
- feat: Claude Swarm Lite — пульт для нескольких сессий Claude Code

